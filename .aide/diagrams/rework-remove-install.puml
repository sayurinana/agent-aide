@startuml rework-remove-install
skinparam defaultFontName "Arial"
skinparam dpi 300
scale 0.5

title 返工任务执行流程：移除 install 相关内容

start

partition "任务3: 删除命令文件" {
  :删除 install-linux.md;
  :删除 install-win.md;
}

partition "任务4: 删除 offline-installer" {
  :删除 linux/ 目录;
  :删除 windows/ 目录;
  :删除 offline-installer/ 空目录;
}

partition "任务5: 清理文档引用" {
  :搜索 install-linux 引用;
  :搜索 install-win 引用;
  :搜索 offline-installer 引用;
  if (发现引用?) then (是)
    :更新/移除引用;
  else (否)
  endif
}

partition "任务6: 修改 back-part 警告" {
  :定位 flow_cmd.py;
  :在 back-part 输出 key 前;
  :添加 rework skill 学习警告;
}

:验证所有修改;

stop

@enduml
