# 实现任务清单

## 1. Git 仓库初始化

- [x] 1.1 在 `cli/init.rs` 添加 `ensure_git_repo` 函数
- [x] 1.2 检测当前目录是否在 git 仓库中
- [x] 1.3 若不在仓库中且 git 可用，执行 `git init`
- [x] 1.4 执行 `git add .` 并创建初始提交
- [x] 1.5 若 git 不可用，输出警告并继续文件初始化

## 2. 常驻分支创建

- [x] 2.1 读取 `branch.resident` 配置（默认 `dev`）
- [x] 2.2 检测常驻分支是否已存在
- [x] 2.3 若不存在，创建并切换到常驻分支
- [x] 2.4 若已存在，切换到常驻分支
- [x] 2.5 处理空仓库情况（无提交时重命名当前分支）

## 3. 任务描述文档创建

- [x] 3.1 读取 `task.description_file` 配置（默认 `task-now.md`）
- [x] 3.2 读取 `task.template` 配置（默认 `任务口述模板.md`）
- [x] 3.3 若描述文件不存在，从模板复制
- [x] 3.4 若描述文件已存在，跳过

## 4. 验证

- [x] 4.1 运行 `cargo test` 确保测试通过（29 passed）
- [x] 4.2 手动测试非 git 目录初始化
- [x] 4.3 手动测试已有 git 仓库初始化
- [x] 4.4 验证常驻分支创建和切换