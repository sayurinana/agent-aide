我期望的是，起始时的提交是为了保证工作仓库状态干净同时保存版本，要记录，但起始它不是flow的一环，它在start之前，
而后面的finish时的提交，完成之后要记录finish时的提交哈希到flow数据中，同时把这个记录为该工作分支的结束分支，
而且flow到这里其实已经结束了，后续的过程是清理也仅仅只是单纯地清理，
我希望能在工作分支aide/NNN完成删除多余文件和收尾的清理提交，
然后在工作仓库干净的情况下把这个工作分支合并到原分支的起始提交处然后把从起始提交到结束提交中的所有提交都进行squash压缩，
最后在原分支的git历史中应该只能看到3个提交，起始、结束、收尾，且此时仓库状态干净